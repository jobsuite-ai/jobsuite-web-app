'use client';

import { MySchedulePage } from '@/components/Employee/MySchedulePage';

/** Personal schedule for managers/admins (employees use `/` as their home). */
export default function MyScheduleRoutePage() {
  return <MySchedulePage />;
}
